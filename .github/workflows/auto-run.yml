# 自动运行调度 (GitHub Actions)
# 每天北京时间 06:00 / 12:00 / 18:00 / 00:00 执行
# 需要先在 GitHub 仓库设置 Secrets > Actions > 添加 OPENAI_API_KEY

name: AI Hot News Factory

on:
  schedule:
    - cron: "0 22,4,10,16 * * *"   # UTC 时间 = 北京时间 - 8小时
  workflow_dispatch:                 # 支持手动触发

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt pytest

      - name: Run tests
        run: python -m pytest tests/ -v

  generate:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run pipeline (all categories)
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          for cat in tech finance business entertainment literature world zhongyi; do
            echo "=== Running: $cat ==="
            python main.py --category "$cat" || echo "⚠️ $cat failed"
          done

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs
          publish_branch: gh-pages
